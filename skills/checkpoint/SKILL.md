---
name: checkpoint
description: Manually save progress as a git WIP checkpoint
---

# Checkpoint Skill

Executes a manual Git WIP checkpoint:

```bash
git add -A && git commit -m "wip(checkpoint): manual progress save"
```
